# Performance traces

Local Instruments `.trace` exports and large XML dumps from the CAOCAP iOS performance audit.

- Keep `metrics.json` (summarized numbers) under version control.
- Re-record traces on device with the commands in [`../PERFORMANCE_AUDIT.md`](../PERFORMANCE_AUDIT.md).
- Filter Console / Instruments Points of Interest by subsystem `com.caocap.app`, category `Performance`.
